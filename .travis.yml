language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gulp-angular-returns' ]; then cd .. && eval "mv $currentfolder generator-gulp-angular-returns" && cd generator-gulp-angular-returns; fi

